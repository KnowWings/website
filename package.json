{
  "name": "knowwings-website",
  "private": true,
  "scripts": {
    "build": "node scripts/build-vercel.mjs",
    "postinstall": "node scripts/build-vercel.mjs",
    "deploy": "npm run build && wrangler deploy"
  }
}
